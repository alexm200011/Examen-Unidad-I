@model IEnumerable<BEUBiblioteca.tbl_Video>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_publicacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.duracion_minutos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblCategoria.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_publicacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.duracion_minutos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblCategoria.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idVideo }) |
            @Html.ActionLink("Details", "Details", new { id=item.idVideo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idVideo })
        </td>
    </tr>
}

</table>
